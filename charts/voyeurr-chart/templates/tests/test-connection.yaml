apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voyeurr.fullname" . }}-test-connection"
  labels:
    {{- include "voyeurr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voyeurr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
